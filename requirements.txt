# ============================================================
# Email Agent - 依赖列表
# ============================================================
# 规则引擎模式（无 AI）只需要: pip install python-dotenv
# 如需 AI 增强/向量记忆，全部安装: pip install -r requirements.txt
# ============================================================

# --- 核心（规则引擎必须）---
python-dotenv>=1.0.0

# --- AI 增强（可选）---
# langchain>=0.3.0
# langchain-openai>=0.2.0
# langchain-chroma>=0.1.0
# openai>=1.0.0
# google-generativeai>=0.8.0

# --- 向量记忆（可选）---
# chromadb>=0.5.0
